export enum ImagePickerActions {
  CAMERA = 'CAMERA',
  GALLERY = 'GALLERY',
}

export enum PermissionResult {
  GRANTED = 1,
  DENIED = 0,
  BLOCKED = -1,
}

export enum Gender {
  MALE = 'MALE',
  FEMALE = 'FEMALE',
}
